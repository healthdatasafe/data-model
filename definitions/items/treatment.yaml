treatment-basic:
  version: v1
  label: Treatment (free-text)
  description: Free-text clinical treatment regimen — IVF, chemotherapy, CBT, dialysis, etc. Used for both aggregate intake (Flavour A — count of lifetime occurrences) and per-cycle/per-course capture (Flavour B). Clinical-domain context (fertility, oncology, …) is carried by the event's streamId via context-via-substream resolution.
  streamId: treatment
  eventType: treatment/basic
  type: composite
  composite:
    name:
      label: Treatment name
      type: text
    count:
      label: Number of times (lifetime)
      type: number
      canBeNull: true
    notes:
      label: Notes
      type: text
      canBeNull: true
  repeatable: unlimited

treatment-coded:
  version: v1
  label: Treatment (coded)
  description: Search and select a coded clinical treatment from datasets service (SNOMED-CT procedure / ICD-10-PCS / CPT). Clinical-domain context is carried by the event's streamId.
  streamId: treatment
  eventType: treatment/coded-v1
  type: datasource-search
  datasource: treatment
  repeatable: unlimited
